id: LibreLoRa_decode
label: decode
category: '[Librelora]'
templates:
  imports: import LibreLoRa
  make: LibreLoRa.decode(${CR})
  callbacks:
  - setCR(${CR})
parameters:
- id: CR
  label: Cr
  dtype: raw
inputs:
- label: in
  domain: stream
  dtype: byte
  vlen: 12
outputs:
- label: out
  domain: stream
  dtype: byte
  vlen: 12
file_format: 1
