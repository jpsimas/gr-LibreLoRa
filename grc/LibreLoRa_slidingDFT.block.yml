id: LibreLoRa_slidingDFT
label: slidingDFT
category: '[Librelora]'
templates:
  imports: import LibreLoRa
  make: LibreLoRa.slidingDFT(${DFTLength}, ${beta})
parameters:
- id: DFTLength
  label: Dftlength
  dtype: raw
- id: beta
  label: beta
  dtype: float
inputs:
- label: in
  domain: stream
  dtype: complex
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ DFTLength }
file_format: 1
